server {
    listen 80;
    server_name _;

    # Forward client IP
    proxy_set_header Host              $host;
    proxy_set_header X-Real-IP         $remote_addr;
    proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    location / {
        proxy_pass http://minurl:8888;

        # Required for redirect responses (302) from minurl
        proxy_redirect off;
    }
}

# To enable HTTPS, add a second server block below and mount
# your certificates to /etc/nginx/certs/ in the compose file:
#
# server {
#     listen 443 ssl;
#     server_name example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     proxy_set_header Host              $host;
#     proxy_set_header X-Real-IP         $remote_addr;
#     proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#
#     location / {
#         proxy_pass http://minurl:8888;
#         proxy_redirect off;
#     }
# }
